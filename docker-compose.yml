# NAS-OS Docker Compose
# ⚠️ 注意：生产环境建议直接安装在宿主机（需要磁盘设备访问）
# 此配置适用于开发测试或受限场景
#
# 镜像地址: ghcr.io/nas-os/nas-os
#
<<<<<<< HEAD
# 当前版本: v2.253.11
=======
# 当前版本: v2.253.11
>>>>>>> 5a397e1 (fix(lint): 修复 47 个 revive linter 错误)

services:
  nas-os:
    image: ghcr.io/nas-os/nas-os:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nas-os
    restart: unless-stopped
    
    # 服务标签
    labels:
<<<<<<< HEAD
      - "nas-os.version=2.253.11"
=======
      - "nas-os.version=2.253.7"
>>>>>>> 5a397e1 (fix(lint): 修复 47 个 revive linter 错误)
      - "nas-os.description=NAS-OS Storage Management System"
      - "nas-os.maintainer=工部"
    
    # 使用 init 进程管理子进程
    init: true
    
    # 优雅停止时间
    stop_grace_period: 30s
    
    # ⚠️ 特权模式（访问磁盘设备）
    # 生产环境建议使用 --device 精确授权
    privileged: true
    
    # 网络模式：host 以获得最佳性能
    # 或使用 bridge + 端口映射
    network_mode: host
    
    # 卷挂载
    volumes:
      # 配置文件
      - ./configs:/etc/nas-os:ro
      - nas-os-data:/var/lib/nas-os
      
      # 存储池（根据实际磁盘挂载点调整）
      # - /mnt/disk1:/mnt/disk1
      # - /mnt/disk2:/mnt/disk2
      
      # 日志
      - ./logs:/var/log/nas-os
    
    # 环境变量
    environment:
      - NAS_OS_CONFIG=/etc/nas-os/config.yaml
      - NAS_OS_DATA_ROOT=/var/lib/nas-os
<<<<<<< HEAD
      - NAS_OS_VERSION=2.253.11
=======
      - NAS_OS_VERSION=2.253.7
>>>>>>> 5a397e1 (fix(lint): 修复 47 个 revive linter 错误)
      # 时区设置
      - TZ=Asia/Shanghai
      # 日志配置
      - NAS_OS_LOG_LEVEL=${LOG_LEVEL:-info}
      # 监控配置
      - NAS_OS_METRICS_ENABLED=true
      # 备份配置
      - NAS_OS_BACKUP_DIR=/var/lib/nas-os/backups
      # 调试模式
      - NAS_OS_DEBUG=${DEBUG:-false}
    
    # 设备访问（非特权模式下使用）
    # devices:
    #   - /dev/sda:/dev/sda
    #   - /dev/sdb:/dev/sdb
    
    # 资源限制（推荐配置）
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    
    # 日志配置
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    
    # 健康检查（覆盖 Dockerfile 中的设置）
    # v2.136.0: distroless 镜像无 shell，必须使用 CMD 而非 CMD-SHELL
    # 注意：如使用 Dockerfile.full (alpine)，可改为 wget 或 curl
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s

  # ========== 可选：配套服务 ==========
  # 取消注释以下服务以启用
  
  # Watchtower 自动更新
  # watchtower:
  #   image: containrrr/watchtower
  #   container_name: watchtower
  #   restart: unless-stopped
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #   command: --interval 86400 --cleanup nas-os
  
  # Prometheus 监控（可选）
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: nas-prometheus
  #   restart: unless-stopped
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheus-data:/prometheus
  #   ports:
  #     - "9090:9090"
  
  # Grafana 可视化（可选）
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: nas-grafana
  #   restart: unless-stopped
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin123

# ========== 网络配置 ==========
networks:
  default:
    name: nas-os-network
    driver: bridge

# ========== 卷配置 ==========
volumes:
  nas-os-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /var/lib/nas-os
  
  # prometheus-data:
  # grafana-data: