# 安全审计报告 Round211

**审计日期**: 2026-04-09  
**审计人员**: 刑部（法务合规）  
**代码库**: NAS-OS  

---

## 一、扫描概览

### 1.1 gosec 静态分析结果

| 指标 | 数量 |
|------|------|
| **总问题数** | 1444 |
| **高危 (HIGH)** | 289 |
| **中危 (MEDIUM)** | 1008 |
| **低危 (LOW)** | 147 |

### 1.2 govulncheck 依赖漏洞扫描

| 指标 | 状态 |
|------|------|
| **已知漏洞 (OSV)** | 0 |
| **扫描模式** | symbol |
| **Go版本** | go1.26.1 |
| **漏洞数据库** | 2026-04-08 更新 |

**结论**: 当前依赖库无已知安全漏洞 ✅

---

## 二、高危问题详情

### 2.1 G115 - 整数溢出 (164个)

**风险等级**: HIGH  
**CWE**: CWE-190 (Integer Overflow)  

**主要影响文件**:
- `internal/webshare/bleve_index.go` - 搜索结果计数转换
- `internal/storagepool/visualization_api.go` - 存储池容量计算
- `internal/storage/raidz_service.go` - RAIDZ磁盘容量计算

**示例**:
```go
// bleve_index.go:974
Total: int64(searchResult.Total)  // uint64 -> int64 可能溢出
```

**修复建议**:
1. 使用安全转换函数检查边界值
2. 对超大数值场景添加溢出检测
3. 考虑使用 `uint64` 类型保持一致性

### 2.2 G402 - TLS证书验证跳过 (4个)

**风险等级**: HIGH  
**CWE**: CWE-295 (Improper Certificate Validation)  

**问题位置**:
| 文件 | 行号 | 描述 |
|------|------|------|
| `internal/media/scraper_enhanced.go` | 81 | 确定跳过验证 (HIGH confidence) |
| `internal/network/tunnel/fnconnect.go` | 167 | 配置控制跳过 |
| `internal/cluster/discovery.go` | 193 | TLS未启用时跳过 |
| `internal/auth/ldap.go` | 159 | LDAP配置控制跳过 |

**修复建议**:
1. **scraper_enhanced.go**: 移除硬编码的 `InsecureSkipVerify: true`
2. 其他文件需确保配置默认值为 `false`
3. 添加安全警告日志，记录跳过验证的情况

### 2.3 G101 - 硬编码凭证嫌疑 (14个)

**风险等级**: HIGH (但置信度 LOW)  
**CWE**: CWE-798 (Hardcoded Credentials)  

**分析结果**:
- 11个为 **OAuth2 Token URL** - **误报** (这些是公开的OAuth端点，非凭证)
- 3个为变量名包含 `token`/`secret` 等关键词 - 需人工复核

**误报示例**:
```go
// oauth2_service.go:172
TokenURL: "https://oauth2.googleapis.com/token"  // 公开端点，非凭证
```

**需复核项**:
- `internal/office/types.go:583` - `ErrInvalidToken` 错误常量
- `internal/auth/oauth2.go:382-393` - OAuth配置函数参数

**修复建议**:
1. 对误报添加 `#nosec G101` 注释
2. 确保 OAuth2 clientID/Secret 通过配置文件注入
3. 定期审查真实凭证暴露风险

---

## 三、中危问题详情

### 3.1 G304 - 路径遍历 (390个)

**风险等级**: MEDIUM  
**CWE**: CWE-22 (Path Traversal)  

**典型场景**: 
- 文件操作中使用用户输入拼接路径
- `filepath.Join` 未验证输入

**修复建议**:
1. 使用 `filepath.Clean` 规范化路径
2. 添加路径边界验证
3. 限制允许的路径前缀

### 3.2 G204 - 命令注入 (314个)

**风险等级**: MEDIUM  
**CWE**: CWE-78 (OS Command Injection)  

**典型场景**:
- ZFS命令执行 (`zpool`, `zfs`)
- 系统工具调用 (`lsblk`, `smartctl`)
- Docker命令执行

**示例**:
```go
// raidz_expansion.go:1077
cmd := exec.CommandContext(ctx, "zpool", "status", "-P", "-v", poolName)
```

**修复建议**:
1. 严格验证传入参数格式
2. 使用参数数组而非字符串拼接
3. 对poolName等变量添加白名单验证

### 3.3 G306 - 文件权限设置 (215个)

**风险等级**: MEDIUM  
**问题**: 创建文件时未设置安全权限

**修复建议**:
1. 使用 `os.OpenFile` 指定权限 (如 0644)
2. 对敏感文件使用更严格权限 (如 0600)

---

## 四、低危问题详情

### 4.1 G104 - 未检查错误 (145个)

**风险等级**: LOW  
**问题**: 忽略函数返回的错误值

**修复建议**:
1. 添加错误处理逻辑
2. 至少记录错误日志

### 4.2 G301 - SQL查询权限检查缺失 (39个)

**风险等级**: MEDIUM  
**问题**: 数据库查询未验证权限

---

## 五、合规状态评估

### 5.1 安全合规矩阵

| 检查项 | 状态 | 说明 |
|--------|------|------|
| SQL注入风险 | ⚠️ 中危 | 需复核G301相关问题 |
| 路径遍历漏洞 | ⚠️ 中危 | 390个G304问题需修复 |
| 命令注入 | ⚠️ 中危 | 314个G204问题需审查 |
| 硬编码凭证 | ✅ 可控 | 多为误报，已标识 |
| 弱加密算法 | ⚠️ 存在 | G401/G501/G505需复核 |
| 整数溢出 | ⚠️ 高危 | 164个G115问题 |
| TLS证书验证 | ⚠️ 高危 | 4个G402问题 |
| 依赖漏洞 | ✅ 无风险 | govulncheck未发现漏洞 |

### 5.2 整体合规评级

**评级**: **B级 (需改进)**

| 维度 | 得分 |
|------|------|
| 依赖安全 | 95/100 |
| 代码安全 | 65/100 |
| 配置安全 | 70/100 |
| **综合** | **72/100** |

---

## 六、优先修复清单

### 🔴 高优先级 (本周修复)

1. **G402-001**: `internal/media/scraper_enhanced.go:81` - 移除硬编码 TLS跳过
2. **G402系列**: 确保其他TLS跳过配置默认为安全值
3. **G115关键**: 存储池容量计算溢出风险

### 🟡 中优先级 (本月修复)

1. G304路径遍历 - 高频使用场景
2. G204命令注入 - ZFS命令参数验证
3. G306文件权限 - 敏感文件创建

### 🟢 低优先级 (季度改进)

1. G104错误处理完善
2. 误报添加 `#nosec` 注释
3. 安全编码规范培训

---

## 七、附录

### A. 问题分布统计

| 规则ID | 数量 | 描述 |
|--------|------|------|
| G115 | 164 | 整数溢出 |
| G204 | 314 | 命令注入 |
| G304 | 390 | 路径遍历 |
| G306 | 215 | 文件权限 |
| G104 | 145 | 未检查错误 |
| G301 | 39 | SQL权限 |
| G101 | 14 | 硬编码凭证 |
| G402 | 4 | TLS跳过验证 |
| 其他 | 98 | 其他问题 |

### B. 扫描配置

- gosec: v2 (最新版)
- govulncheck: v1.1.4
- 扫描范围: 全代码库 `./...`
- 输出格式: JSON

---

**审计完成时间**: 2026-04-09 17:02  
**下次审计建议**: Round212，重点跟踪G402和G115修复进度