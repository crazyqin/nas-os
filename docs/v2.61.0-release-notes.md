# NAS-OS v2.61.0 发布说明

**发布日期**: 2026-03-15  
**版本类型**: Stable

---

## 更新内容

### 文档体系完善 (礼部)

本次版本主要聚焦于文档体系的完善和优化：

#### README.md 更新
- 版本号同步至 v2.61.0
- 更新下载链接和 Docker 镜像版本

#### 用户指南优化
- 新增 `docs/user-guide/README.md` 索引文件
- 更新所有用户指南版本号
- 优化文档结构和导航

#### API 文档补充
- 新增 `docs/api/storage-api.md` 存储 API 文档
- 新增 `docs/api/user-api.md` 用户 API 文档
- 更新 API 文档索引

### CHANGELOG 更新
- 添加 v2.61.0 版本变更记录

---

## 升级说明

### Docker 升级

```bash
# 拉取新版本
docker pull ghcr.io/crazyqin/nas-os:v2.61.0

# 停止旧容器
docker stop nasd

# 启动新容器
docker run -d \
  --name nasd \
  --restart unless-stopped \
  -p 8080:8080 \
  -v /data:/data \
  -v /etc/nas-os:/config \
  ghcr.io/crazyqin/nas-os:v2.61.0
```

### 二进制升级

```bash
# 下载新版本
wget https://github.com/crazyqin/nas-os/releases/download/v2.61.0/nasd-linux-amd64

# 替换旧版本
chmod +x nasd-linux-amd64
sudo mv nasd-linux-amd64 /usr/local/bin/nasd

# 重启服务
sudo systemctl restart nasd
```

---

## 相关链接

- [完整更新日志](../CHANGELOG.md)
- [API 文档](../docs/api/README.md)
- [用户指南](../docs/user-guide/README.md)

---

*NAS-OS 团队*