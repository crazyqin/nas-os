# Follow-up fixes — 2026-07-25

## P0
- app-center: normalize Authorization (no double Bearer); api.js interceptor also collapses `Bearer Bearer`
- Uses getAuthToken when available

## P1
- `make install`: never overwrites existing `/etc/nas-os/config.yaml` (writes `.example` instead)
- Swagger: off when `NAS_OS_ENV=production|prod` unless `NAS_OS_SWAGGER=1`; force off with `=0`
- Audit log: long-lived file + mkdir; only mutating methods on sensitive paths; `NAS_OS_AUDIT_LOG` override
- docker-compose.prod: monitoring ports on 127.0.0.1; Grafana password required; docker.sock opt-in; cAdvisor profile+non-privileged docker_only

## P2
- sw.js cache → v3.24.3
- storage.html dropped redundant second fetch wrapper
