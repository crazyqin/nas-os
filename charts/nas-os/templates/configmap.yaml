apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nas-os.fullname" . }}-config
  labels:
    {{- include "nas-os.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config.content | nindent 4 }}