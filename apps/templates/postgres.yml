# PostgreSQL - 高级关系型数据库
#
# 用途：
# - 应用数据库
# - 数据持久化
# - 支持复杂查询和事务
#
# 数据目录：/var/lib/nas-os/data/postgres

services:
  postgres:
    image: postgres:16-alpine
    container_name: nas-postgres
    restart: unless-stopped
    
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nasadmin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-naspass123}
      POSTGRES_DB: ${POSTGRES_DB:-nasdb}
      # 性能优化
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
      PGDATA: /var/lib/postgresql/data/pgdata
    
    ports:
      - "5432:5432"
    
    volumes:
      # 数据持久化
      - ${NAS_DATA_DIR:-/var/lib/nas-os/data}/postgres:/var/lib/postgresql/data
      # 初始化脚本
      - ${NAS_CONFIG_DIR:-/var/lib/nas-os/config}/postgres/init:/docker-entrypoint-initdb.d:ro
    
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nasadmin} -d ${POSTGRES_DB:-nasdb}"]
      interval: 30s
      timeout: 10s
      retries: 3
    
    labels:
      - "nas-os.app=postgres"
      - "nas-os.category=database"
      - "nas-os.description=Advanced relational database"

# 可选：PostgreSQL 管理 UI (pgAdmin)
# pgadmin:
#   image: dpage/pgadmin4:latest
#   container_name: nas-pgadmin
#   restart: unless-stopped
#   environment:
#     PGADMIN_DEFAULT_EMAIL: admin@nas.local
#     PGADMIN_DEFAULT_PASSWORD: admin123
#     PGADMIN_LISTEN_PORT: 5050
#   ports:
#     - "5050:5050"
#   volumes:
#     - ${NAS_DATA_DIR}/pgadmin:/var/lib/pgadmin
#   depends_on:
#     - postgres